using UnityEngine;
using System.Collections;

// helper class that will spawn a zone prefab in a desired position
public class ZoneSpawner : MonoBehaviour {

	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
	
	}

    void SpawnZone(Vector3 position, Vector3 size) {

    }
}
