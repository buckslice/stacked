This a placeholder file, since Git will not sync empty folders. This causes problems with Unity's .meta files, which ARE synced by Git, even for empty files.

This folder is the output directory for all script-created assets.